# full absolute path to database file
# you can not use ~ or other home directory redirect since the scoreboard app may be run as root for port 80 privilages
SCORING_DB_PATH = "/home/<user>/database/scoring.db"

